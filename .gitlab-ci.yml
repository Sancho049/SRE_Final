image: python:3.9

stages:
  - test
  - deploy

test:
  stage: test
  variables:
    CI: "true"
  script:
    - pip install --upgrade pip
    - pip install -r requirements.txt
    - python myblog/manage.py test myblog

deploy:
  stage: deploy
  script:
    - echo "Deploying application..."
